# setuptools
[dependency-groups]
dev = [
    "setuptools>=75.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
